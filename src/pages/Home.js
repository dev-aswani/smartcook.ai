//This page is the Home/Dishes page of the application, which imports the necessary containers required to read and update user input related to the dish names and cooking times and renders them whenever the user launches the application or navigates to the Dishes section

import React from "react";
import { HomeContainer } from "../containers";
export const Home = () => {
	return (
		<>
			<HomeContainer />
		</>
	);
};
